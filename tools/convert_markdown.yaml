identity:
  author: enconvert
  name: convert_markdown
  label:
    en_US: Convert File to Markdown
description:
  human:
    en_US: Convert an uploaded file (Word, PowerPoint, Excel, PDF, EPUB, HTML and more) into Markdown.
  llm: Convert an uploaded file to Markdown with EnConvert. Returns a download URL for the Markdown result. Use this to turn documents into agent-readable text.
parameters:
  - name: file
    type: file
    required: true
    form: llm
    label:
      en_US: File
    human_description:
      en_US: The file to convert. Wire in an uploaded file or a file variable from an earlier node.
    llm_description: The file to convert. Required.
extra:
  python:
    source: tools/convert_markdown.py
